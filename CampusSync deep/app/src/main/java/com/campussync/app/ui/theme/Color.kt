package com.campussync.app.ui.theme

import androidx.compose.ui.graphics.Color

// Background & Surface (The "Let it Breathe" canvas)
val OffWhiteBackground = Color(0xFFF8FAFC)
val SurfaceWhite = Color(0xFFFFFFFF)

// Primary Brand
val BrandPrimary = Color(0xFF4F46E5) // Indigo
val BrandPrimaryLight = Color(0xFFEEF2FF) // Tinted background for primary icons

// Text & Typography
val TextPrimary = Color(0xFF0F172A) // Slate 900
val TextSecondary = Color(0xFF64748B) // Slate 500
val TextTertiary = Color(0xFF94A3B8) // Slate 400

// Semantic Colors (Status)
val StatusSuccess = Color(0xFF10B981) // Emerald Green
val StatusSuccessBackground = Color(0xFFD1FAE5)

val StatusWarning = Color(0xFFF59E0B) // Amber
val StatusWarningBackground = Color(0xFFFEF3C7)

val StatusError = Color(0xFFEF4444) // Soft Crimson
val StatusErrorBackground = Color(0xFFFEE2E2)

val StatusInfo = Color(0xFF3B82F6) // Soft Blue
val StatusInfoBackground = Color(0xFFDBEAFE)

// Outlines & Dividers
val BorderLight = Color(0xFFE2E8F0) // Slate 200
